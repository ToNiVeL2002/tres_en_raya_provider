import 'package:flutter/material.dart';

class EstilosScreen extends StatelessWidget {
   
  const EstilosScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('EstilosScreen'),
      ),
    );
  }
}